# أين يتم حفظ البيانات في النظام

## 📍 موقع حفظ البيانات

### 1. **قاعدة البيانات الرئيسية (Production)**
البيانات محفوظة في **PostgreSQL Database على Render.com**:
- **الموقع**: خادم Render.com (Cloud)
- **نوع قاعدة البيانات**: PostgreSQL
- **اسم قاعدة البيانات**: `hospital_db_j0zf` (أو حسب ما قمت بإنشائه)
- **العنوان**: `dpg-d5loemdactks73brk6dg-a` (Render internal host)

### 2. **كيفية الوصول إلى البيانات**

#### أ. **من التطبيق (Frontend)**
- البيانات يتم جلبها عبر API Backend
- الموقع: `https://hospital-api-7v73.onrender.com`
- جميع البيانات تمر عبر Backend API

#### ب. **من Backend**
- يستخدم Prisma ORM للوصول إلى قاعدة البيانات
- اتصال عبر `DATABASE_URL` environment variable

#### ج. **من Render Dashboard**
1. اذهب إلى: **https://dashboard.render.com**
2. اختر **PostgreSQL Database** service (`hospital-db`)
3. اضغط على **"Connect"** لرؤية معلومات الاتصال
4. يمكنك استخدام **"Shell"** للوصول المباشر

#### د. **استخدام Prisma Studio (محلياً)**
```bash
# في مجلد المشروع
npx prisma studio
```
سيفتح واجهة رسومية لعرض وتعديل البيانات

### 3. **البيانات المحفوظة محلياً (Development)**

إذا كنت تعمل محلياً:
- **قاعدة البيانات**: SQLite
- **الملف**: `database.sqlite` في المجلد الرئيسي
- **المكان**: `/Users/haider.m/Desktop/project/hosptal/database.sqlite`

> **ملاحظة**: في Production (على Render)، البيانات **ليست** محفوظة في ملف محلي، بل في قاعدة بيانات PostgreSQL على السحابة.

### 4. **ما هي البيانات المحفوظة؟**

البيانات التالية محفوظة في قاعدة البيانات:

#### **الجدول الرئيسية:**
- ✅ `users` - المستخدمون (موظفون، أطباء، صيادلة، إلخ)
- ✅ `patients` - المرضى
- ✅ `visits` - الزيارات
- ✅ `lab_results` - نتائج التحاليل
- ✅ `pharmacy_prescriptions` - الوصفات الطبية
- ✅ `diagnoses` - التشخيصات
- ✅ `notifications` - الإشعارات
- ✅ `activity_log` - سجل النشاطات
- ✅ `lab_tests_catalog` - كتالوج التحاليل
- ✅ `drugs_catalog` - كتالوج الأدوية
- ✅ `prescription_sets` - مجموعات الوصفات
- ✅ `roles` - الأدوار
- ✅ `permissions` - الصلاحيات
- ✅ `visit_attachments` - المرفقات

### 5. **كيفية التحقق من البيانات**

#### **من Backend Logs:**
اذهب إلى Render Dashboard → `hospital-api` → **"Logs"**
سترى رسائل مثل:
```
Database tables exist, migrations already applied
Default roles and users created successfully
```

#### **من Prisma Studio:**
```bash
# تأكد من أن DATABASE_URL صحيح في .env
npx prisma studio
```

#### **من Render PostgreSQL Shell:**
```bash
# في Render Dashboard → PostgreSQL → Shell
psql $DATABASE_URL

# ثم استخدم SQL queries:
\dt                    # عرض جميع الجداول
SELECT * FROM patients; # عرض المرضى
SELECT * FROM users;    # عرض المستخدمين
```

### 6. **معلومات الاتصال (DATABASE_URL)**

يتم حفظ `DATABASE_URL` في:
- **Render**: Environment Variables → `DATABASE_URL`
- **محلياً**: ملف `.env` في المجلد الرئيسي

صيغة DATABASE_URL:
```
postgresql://user:password@host:port/database?sslmode=require
```

### 7. **النسخ الاحتياطي (Backup)**

Render يقوم بنسخ احتياطي تلقائي لـ PostgreSQL:
- **النسخ الاحتياطي**: يومي (على خطط مدفوعة)
- **الموقع**: Render Dashboard → PostgreSQL → "Backups"

### 8. **الملفات المحفوظة محلياً**

- ❌ **لا يوجد ملفات محلية** للبيانات في Production
- ✅ **جميع البيانات على السحابة** (Render PostgreSQL)
- ✅ **الملفات المحلية فقط للتطوير** (`database.sqlite`)

---

## 📊 ملخص

| البيئة | نوع قاعدة البيانات | الموقع |
|--------|-------------------|--------|
| **Production** (Render) | PostgreSQL | السحابة (Render) |
| **Development** (محلياً) | SQLite | `database.sqlite` |

**جميع بيانات المستخدمين والمرضى والزيارات محفوظة على قاعدة بيانات PostgreSQL على Render.com**
