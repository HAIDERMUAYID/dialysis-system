# إعداد Seed Data

## ⚠️ قبل التشغيل

تأكد من وجود `DATABASE_URL` في أحد الملفات التالية:

### الخيار 1: `prisma/.env` (موصى به)
```env
DATABASE_URL="postgresql://postgres.xxxxx:your_password@aws-0-region.pooler.supabase.com:6543/postgres"
```

### الخيار 2: `.env` في الجذر
```env
DATABASE_URL="postgresql://postgres.xxxxx:your_password@aws-0-region.pooler.supabase.com:6543/postgres"
```

## 🚀 التشغيل

بعد التأكد من وجود `DATABASE_URL`:

```bash
npm run prisma:seed
```

أو:

```bash
node prisma/seed.js
```

## ✅ بعد التشغيل

ستحصل على:
- 7 مستخدمين (جميعهم بكلمة مرور: `123456`)
- 5 مرضى
- 5 زيارات بحالات مختلفة
- بيانات تجريبية كاملة

## 🔐 معلومات الدخول

**جميع المستخدمين: كلمة المرور = `123456`**

- `admin` - مدير النظام
- `inquiry` - موظف الاستعلامات
- `lab` - موظف التحاليل
- `lab_manager` - مدير المختبر
- `pharmacist` - الصيدلي
- `pharmacy_manager` - مدير الصيدلية
- `doctor` - الطبيب
