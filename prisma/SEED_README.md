# دليل إضافة البيانات التجريبية (Seed Data)

## 📋 نظرة عامة

هذا الملف يحتوي على بيانات تجريبية شاملة للنظام لاختباره. يتم إضافة:

- ✅ 7 أدوار (Roles)
- ✅ 7 مستخدمين (Users) - جميعهم بكلمة مرور: `123456`
- ✅ 5 تحاليل في الكتالوج (Lab Tests)
- ✅ 5 أدوية في الكتالوج (Drugs)
- ✅ 5 مرضى (Patients)
- ✅ 5 زيارات (Visits) بحالات مختلفة
- ✅ نتائج تحاليل (Lab Results)
- ✅ وصفات طبية (Prescriptions)
- ✅ تشخيصات (Diagnoses)
- ✅ سجل أنشطة (Activity Logs)
- ✅ إشعارات (Notifications)

## 🚀 كيفية التشغيل

### الطريقة 1: استخدام npm script
```bash
npm run prisma:seed
```

### الطريقة 2: تشغيل مباشر
```bash
node prisma/seed.js
```

### الطريقة 3: استخدام Prisma CLI
```bash
npx prisma db seed
```

## 🔐 معلومات الدخول

**جميع المستخدمين يستخدمون نفس كلمة المرور: `123456`**

| اسم المستخدم | الدور | الاسم |
|-------------|------|------|
| `admin` | مدير النظام | أحمد محمد |
| `inquiry` | موظف الاستعلامات | فاطمة علي |
| `lab` | موظف التحاليل | خالد حسن |
| `lab_manager` | مدير المختبر | سارة أحمد |
| `pharmacist` | الصيدلي | محمد خالد |
| `pharmacy_manager` | مدير الصيدلية | نورا سعيد |
| `doctor` | الطبيب | د. علي محمود |

## 📊 البيانات المضافة

### الأدوار (Roles)
- admin (مدير النظام)
- inquiry (موظف الاستعلامات)
- lab (موظف التحاليل)
- lab_manager (مدير المختبر)
- pharmacist (الصيدلي)
- pharmacy_manager (مدير الصيدلية)
- doctor (الطبيب)

### التحاليل (Lab Tests)
1. CBC - تحليل الدم الشامل
2. Glucose - السكر في الدم
3. Cholesterol - الكوليسترول
4. Hemoglobin - الهيموجلوبين
5. Creatinine - الكرياتينين

### الأدوية (Drugs)
1. Paracetamol - باراسيتامول
2. Amoxicillin - أموكسيسيلين
3. Ibuprofen - آيبوبروفين
4. Omeprazole - أوميبرازول
5. Metformin - ميتفورمين

### المرضى (Patients)
1. محمد أحمد العلي
2. فاطمة سعيد الخالد
3. خالد محمود النور
4. نورا علي السالم
5. أحمد حسن المطيري

### الزيارات (Visits)
- زيارة 1: `pending_lab` (بانتظار التحاليل)
- زيارة 2: `pending_pharmacy` (بانتظار الصيدلية)
- زيارة 3: `pending_doctor` (بانتظار الطبيب)
- زيارة 4: `pending_all` (بانتظار جميع الأقسام)
- زيارة 5: `completed` (مكتملة)

## ⚠️ ملاحظات مهمة

1. **البيانات الموجودة**: إذا كانت البيانات موجودة بالفعل (مثل المستخدمين أو المرضى)، سيتم تحديثها بدلاً من إنشاء نسخ مكررة.

2. **كلمات المرور**: جميع كلمات المرور مشفرة باستخدام bcrypt. الكلمة الافتراضية هي `123456`.

3. **العلاقات**: يتم إنشاء البيانات بشكل متسلسل لضمان وجود العلاقات المطلوبة (مثل إنشاء المستخدمين قبل المرضى).

4. **إعادة التشغيل**: يمكنك تشغيل الـ seed عدة مرات بأمان - لن يتم إنشاء بيانات مكررة.

## 🔄 إعادة تعيين البيانات

إذا أردت حذف جميع البيانات وإعادة إضافتها:

```bash
# حذف جميع البيانات (احذر!)
npx prisma migrate reset

# ثم إضافة البيانات التجريبية
npm run prisma:seed
```

## 📝 تخصيص البيانات

يمكنك تعديل ملف `prisma/seed.js` لإضافة أو تعديل البيانات حسب احتياجاتك.

## 🐛 استكشاف الأخطاء

إذا واجهت أخطاء:

1. تأكد من أن قاعدة البيانات متصلة بشكل صحيح
2. تأكد من أن `DATABASE_URL` في `prisma/.env` صحيحة
3. تأكد من تشغيل `npx prisma generate` أولاً
4. تحقق من أن جميع الجداول موجودة في قاعدة البيانات
